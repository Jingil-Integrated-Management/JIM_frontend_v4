import { OutsourceData } from '../types';

const Outsource = (): OutsourceData => ({
  material_price: null,
  milling_price: null,
  heat_treat_price: null,
  wire_price: null,
  material_client: null,
  milling_client: null,
  heat_treat_client: null,
  wire_client: null,
});

export default Outsource;
